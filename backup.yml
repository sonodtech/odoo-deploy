---
# ============================================================================
# Backup Playbook - Backup Database and Filestore to S3
# ============================================================================
- name: Backup Odoo Database and Filestore to S3
  hosts: all
  remote_user: "{{ deploy_user }}"
  gather_facts: no
  
  tasks:
    - name: Include backup tasks
      ansible.builtin.include_tasks: "{{ playbook_dir }}/tasks/backup-db.yml"
